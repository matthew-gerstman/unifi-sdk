# ============================================================================
# Cloud API Configuration (for remote monitoring)
# ============================================================================
# Get your API key from: https://unifi.ui.com → API section
UNIFI_CLOUD_API_KEY=your_cloud_api_key_here

# ============================================================================
# Local Controller API Configuration (for configuration changes)
# ============================================================================
# Only needed if you want to automate configuration changes
# Must be on same network as UDM or connected via VPN/Tailscale

UNIFI_LOCAL_HOST=192.168.1.1
UNIFI_LOCAL_USERNAME=admin
UNIFI_LOCAL_PASSWORD=your_password
UNIFI_LOCAL_SITE=default
UNIFI_LOCAL_PORT=443

# ============================================================================
# Usage Modes
# ============================================================================
# 
# MONITORING ONLY (from anywhere):
# - Set UNIFI_CLOUD_API_KEY only
# - Can monitor network remotely
# - Read-only access
#
# FULL AUTOMATION (from home network):
# - Set both Cloud and Local credentials
# - Can monitor AND configure
# - Full read/write access
#
# CONFIGURATION ONLY (from home network):
# - Set Local credentials only
# - Can configure but limited monitoring
# - Requires network access
